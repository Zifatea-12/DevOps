name: Simple CI

on:
  push:
    branches: [ "main" ]

jobs:
  test-html:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Show files
        run: ls -la

      - name: Pipeline success
        run: echo "CI pipeline berjalan sukses"